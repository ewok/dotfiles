-- https://github.com/
local aux_lspconfig = require("utils.aux.lspconfig")

local M = {
    requires = {
        "metals",
        "aerial",
        "lspconfig",
        "nvim-navic",
    },
}
function M.before() end

function M.load() end

function M.after() end

return M
