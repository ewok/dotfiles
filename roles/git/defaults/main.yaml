---
packages_archlinux:
  - git-crypt
  - git-extras
  - git-filter-repo
  - git-trim
  - pass-git-helper

packages_void:
  - git-crypt
  - pass-git-helper
  - git-extras
  - git-filter-repo
# TODO: install via src
# git-trim
