# styles - set the folder for your theme definition file
set $config $HOME/.config/sway

include $config/theme.conf
include $config/variables.conf
include $config/inputs.conf
include $config/outputs.conf

include $config/modes/*
include $config/config.d/*.conf

# gaps inner 7
# smart_gaps on

# # Font for window titles. Will also be used by the bar unless a different font
# # is used in the bar {} block below.
# font pango:{{ regularFont }} {{ regularFontSize }}
