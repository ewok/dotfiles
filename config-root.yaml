config:
  backup: true
  banner: false
  create: true
  ignoreempty: false
  keepdot: false
  longkey: false
  showdiff: false
  workdir: ~/.config/dotdrop
  dotpath: packages
  import_variables:
    - "local_vars.yaml"
  import_configs:
    - packages/root_backup/config.yaml
    - packages/root_laptop/config.yaml
    - packages/root_sudoers/config.yaml
    - packages/root_vpn/config.yaml
dotfiles:
profiles:
  ewok-t14s:
    include:
      - backup
      - laptop
      - sudoers
      - vpn
    dynvariables:
      hostname: echo ewok-t14s
      _dotdrop_rootpath: dirname {{@@ _dotdrop_cfgpath @@}}
    variables:
      yay: 'yes | pacman -S --needed --noconfirm'
      suspend_type: suspend
  ewok-rpi:
    include:
      - backup
      - sudoers
    dynvariables:
      hostname: echo ewok-rpi
      _dotdrop_rootpath: dirname {{@@ _dotdrop_cfgpath @@}}
    variables:
      yay: 'yes | pacman -S --needed --noconfirm'
      suspend_type: suspend
